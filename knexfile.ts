import { config } from "./src/database";

export default config;

// O arquivo knexfile.ts é usado pelo Knex.js para configurar as conexões com o banco de dados e as migrações.
// Ele define as configurações necessárias para conectar ao banco de dados e executar as migrações.
